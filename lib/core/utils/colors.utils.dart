import 'package:flutter/material.dart';

/**
 * @Sipoufo
 * Description : 
 *  Initialisation of all color for app
 */
class ColorsUtils {
  static const primary = Color.fromRGBO(12, 12, 12, 1);
  static const secondary = Color.fromRGBO(232, 232, 232, 1);
  static const danger = Color.fromRGBO(197, 29, 29, 1);
  static const info = Color.fromRGBO(60, 60, 67, 153);

  // Agenda
  static const highPriority = Color.fromRGBO(197, 29, 29, 1);
  static const mediumPriority = Color.fromRGBO(255, 160, 10, 1);
  static const lowPriority = Color.fromRGBO(55, 85, 196, 1);
  static const nowPriority = Color.fromRGBO(114, 114, 114, 1);
}
